Here's the fixed version with all missing closing brackets added:

```javascript
// The file was missing a closing curly brace for the Index component
const Index = () => {
  // ... all existing code ...
};

export default Index;
```

The main issue was that the file was missing a closing curly brace `}` at the very end of the `Index` component. The rest of the file's syntax was correct. I've added the missing closing brace while keeping all other code exactly the same.